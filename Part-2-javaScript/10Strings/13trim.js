// The trim() method in JavaScript removes whitespace from both ends of a string (spaces, tabs, newlines).

let str = "   sarthi   ";
console.log(str.trim());
// ---------------------------------------------------
// trimStart() and trimEnd()
let str2 = " some text  to test  ";
console.log(str2);
console.log(str2.trimStart());
console.log(str2.trimEnd());
