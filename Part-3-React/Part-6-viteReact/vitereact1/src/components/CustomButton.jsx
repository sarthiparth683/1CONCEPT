import React from "react";

const CustomButton = () => {
  return (
    <div>
      <h3>Testing Components in React</h3>
    </div>
  );
};

export default CustomButton;

// Prompt in GPt:- Write test cases using jest and react testing library for a React button component named CustomButton. It accepts props: label (string), onClick (function), and style (object). Use Jest and React Testing Library. Include tests for clicking the button and ensuring it displays the correct label.
