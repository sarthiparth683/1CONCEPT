import React from "react";

const Notes = () => {
  return (
    <div>
      <ol>
        <li>installing npm in React</li>
        <li>install node</li>
        <li>node - v -check node version</li>   
        <li>npm create vite@latest . -- --template react -</li>
        <li>cd folder name or cd .. to go back</li>  
        <li>npm install- for installing node modules,Dependencies.</li>
        <li>npm run dev - to start the app</li>
      </ol>

      <ol>
        <li>Hoisting on other devices in vite...</li>
        <li>
          in packaage.json file - need to change -"scripts": "dev": "vite
          --host",
        </li>  
      </ol>

      <ul>
        <li>installing bun in React</li>
        <li>install node</li>
        <li>node - v -check node version</li>
        <li>bun -v - to check version of bun</li>
        <li>npm i -g bun - to install bun globally</li>
        <li>
          powerShell vs gitBash (in powershell need to change set as
          administration)(otherwise it won't work in powershell but it will work
          in gitbash)
        </li>
        <li>after installing bun </li>
        <li>bun create vite</li>
        <li>bun install</li>
        <li>bun run dev</li>
        <li>
          bun install react@rc - to install packages in package.json check
          dependencies "react"
        </li>
        <li>
          bun install react-dom@rc - to install packages in package.json check
          dependencies "react-dom"
        </li>
      </ul>
    </div>
  );
};

export default Notes;
