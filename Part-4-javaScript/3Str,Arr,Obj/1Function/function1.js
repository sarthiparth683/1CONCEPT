function FunctioName(a, b) {   //fuction declaration
    let c = a + b;
    let d = a * b;             // function defination/body
    return console.log(c, d); 
}
FunctioName(2, 5);             // Calling Function

// Parameters and Arguments:
// Return Values:The returned value can be used in expressions or assigned to variables.
// function argumenets :-value passed  eg. add(3, 5); sum(2, 3);
// function parameters :- RECEIVE VALUE eg. FunctionName(a, b);


// local variabel { inside }
// Global variable { outside }
// Parentheses, Function Call Syntax, bracket's used ().


// Parameters:
// Function Placeholders: Parameters are placeholders defined within a function's declaration. They represent the values that will be passed to the function when it's called. These values are called arguments.
// Data Input: Parameters act as variables within the function's scope, allowing the function to receive and work with the provided data.